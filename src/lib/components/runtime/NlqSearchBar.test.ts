// Code generated by Tester Agent. DO NOT EDIT.
// Task: NLQ Phase 1 — NlqSearchBar component tests
// Builder must make these tests pass without modification.

import { describe, it, expect, vi } from "vitest";
import { render, screen, waitFor } from "@testing-library/svelte";
import userEvent from "@testing-library/user-event";
import NlqSearchBar from "./NlqSearchBar.svelte";

describe("NlqSearchBar", () => {
  // ---------------------------------------------------------------------------
  // Rendering
  // ---------------------------------------------------------------------------

  describe("initial render", () => {
    it("should render a search input with the correct placeholder", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
        },
      });

      const input = screen.getByRole("textbox");
      expect(input).toBeInTheDocument();
      expect(input).toHaveAttribute(
        "placeholder",
        expect.stringContaining("knowledge graph"),
      );
    });

    it("should render a search submit button", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
        },
      });

      // Accept either "Search" text or a role=button with accessible name
      const button = screen.getByRole("button", { name: /search/i });
      expect(button).toBeInTheDocument();
    });

    it("should NOT show Back to browse button when inSearchMode is false", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
          inSearchMode: false,
        },
      });

      expect(
        screen.queryByRole("button", { name: /back to browse/i }),
      ).not.toBeInTheDocument();
    });

    it("should NOT show error message when error prop is not set", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
        },
      });

      expect(screen.queryByRole("alert")).not.toBeInTheDocument();
    });

    it("should NOT show loading indicator when loading is false", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
          loading: false,
        },
      });

      expect(
        screen.queryByTestId("nlq-loading-indicator"),
      ).not.toBeInTheDocument();
    });
  });

  // ---------------------------------------------------------------------------
  // Search submission
  // ---------------------------------------------------------------------------

  describe("search submission", () => {
    it("should call onSearch with input value when Enter is pressed", async () => {
      const onSearch = vi.fn();
      const user = userEvent.setup();

      render(NlqSearchBar, {
        props: { onSearch, onClear: vi.fn() },
      });

      const input = screen.getByRole("textbox");
      await user.type(input, "show me all drones{Enter}");

      expect(onSearch).toHaveBeenCalledOnce();
      expect(onSearch).toHaveBeenCalledWith("show me all drones");
    });

    it("should call onSearch with input value when search button is clicked", async () => {
      const onSearch = vi.fn();
      const user = userEvent.setup();

      render(NlqSearchBar, {
        props: { onSearch, onClear: vi.fn() },
      });

      const input = screen.getByRole("textbox");
      await user.type(input, "fleet alpha entities");

      const button = screen.getByRole("button", { name: /search/i });
      await user.click(button);

      expect(onSearch).toHaveBeenCalledOnce();
      expect(onSearch).toHaveBeenCalledWith("fleet alpha entities");
    });

    it("should NOT call onSearch when Enter is pressed with empty input", async () => {
      const onSearch = vi.fn();
      const user = userEvent.setup();

      render(NlqSearchBar, {
        props: { onSearch, onClear: vi.fn() },
      });

      const input = screen.getByRole("textbox");
      await user.type(input, "{Enter}");

      expect(onSearch).not.toHaveBeenCalled();
    });

    it("should NOT call onSearch when search button is clicked with empty input", async () => {
      const onSearch = vi.fn();
      const user = userEvent.setup();

      render(NlqSearchBar, {
        props: { onSearch, onClear: vi.fn() },
      });

      const button = screen.getByRole("button", { name: /search/i });
      await user.click(button);

      expect(onSearch).not.toHaveBeenCalled();
    });

    it("should NOT call onSearch when input is only whitespace", async () => {
      const onSearch = vi.fn();
      const user = userEvent.setup();

      render(NlqSearchBar, {
        props: { onSearch, onClear: vi.fn() },
      });

      const input = screen.getByRole("textbox");
      await user.type(input, "   {Enter}");

      expect(onSearch).not.toHaveBeenCalled();
    });

    it("should pass the trimmed query string to onSearch", async () => {
      const onSearch = vi.fn();
      const user = userEvent.setup();

      render(NlqSearchBar, {
        props: { onSearch, onClear: vi.fn() },
      });

      const input = screen.getByRole("textbox");
      await user.type(input, "  drones in west fleet  {Enter}");

      expect(onSearch).toHaveBeenCalledWith("drones in west fleet");
    });
  });

  // ---------------------------------------------------------------------------
  // Loading state
  // ---------------------------------------------------------------------------

  describe("loading state", () => {
    it("should show a loading indicator when loading is true", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
          loading: true,
        },
      });

      expect(screen.getByTestId("nlq-loading-indicator")).toBeInTheDocument();
    });

    it("should disable the text input while loading", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
          loading: true,
        },
      });

      const input = screen.getByRole("textbox");
      expect(input).toBeDisabled();
    });

    it("should disable the search button while loading", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
          loading: true,
        },
      });

      const button = screen.getByRole("button", { name: /search/i });
      expect(button).toBeDisabled();
    });

    it("should NOT call onSearch when Enter is pressed during loading", async () => {
      const onSearch = vi.fn();
      render(NlqSearchBar, {
        props: {
          onSearch,
          onClear: vi.fn(),
          loading: true,
        },
      });

      // Input is disabled during loading so no key events fire,
      // but guard the expectation regardless
      expect(onSearch).not.toHaveBeenCalled();
    });
  });

  // ---------------------------------------------------------------------------
  // Search mode (inSearchMode)
  // ---------------------------------------------------------------------------

  describe("inSearchMode", () => {
    it("should show Back to browse button when inSearchMode is true", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
          inSearchMode: true,
        },
      });

      expect(
        screen.getByRole("button", { name: /back to browse/i }),
      ).toBeInTheDocument();
    });

    it("should call onClear when Back to browse is clicked", async () => {
      const onClear = vi.fn();
      const user = userEvent.setup();

      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear,
          inSearchMode: true,
        },
      });

      const clearButton = screen.getByRole("button", {
        name: /back to browse/i,
      });
      await user.click(clearButton);

      expect(onClear).toHaveBeenCalledOnce();
    });

    it("should NOT call onSearch when Back to browse is clicked", async () => {
      const onSearch = vi.fn();
      const user = userEvent.setup();

      render(NlqSearchBar, {
        props: {
          onSearch,
          onClear: vi.fn(),
          inSearchMode: true,
        },
      });

      const clearButton = screen.getByRole("button", {
        name: /back to browse/i,
      });
      await user.click(clearButton);

      expect(onSearch).not.toHaveBeenCalled();
    });
  });

  // ---------------------------------------------------------------------------
  // Error display
  // ---------------------------------------------------------------------------

  describe("error display", () => {
    it("should show error message when error prop is set", () => {
      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
          error: "NLQ service unavailable",
        },
      });

      expect(screen.getByRole("alert")).toBeInTheDocument();
      expect(screen.getByRole("alert")).toHaveTextContent(
        "NLQ service unavailable",
      );
    });

    it("should clear the visible error when the user starts typing", async () => {
      const user = userEvent.setup();

      render(NlqSearchBar, {
        props: {
          onSearch: vi.fn(),
          onClear: vi.fn(),
          error: "Previous search failed",
        },
      });

      expect(screen.getByRole("alert")).toBeInTheDocument();

      const input = screen.getByRole("textbox");
      await user.type(input, "n");

      await waitFor(() => {
        expect(screen.queryByRole("alert")).not.toBeInTheDocument();
      });
    });

    it("should display different error messages accurately", () => {
      const errorMessages = [
        "Query timed out",
        "Unable to connect to NLQ service",
        "Internal server error",
      ];

      for (const msg of errorMessages) {
        const { unmount } = render(NlqSearchBar, {
          props: {
            onSearch: vi.fn(),
            onClear: vi.fn(),
            error: msg,
          },
        });

        expect(screen.getByRole("alert")).toHaveTextContent(msg);
        unmount();
      }
    });
  });

  // ---------------------------------------------------------------------------
  // Keyboard accessibility
  // ---------------------------------------------------------------------------

  describe("keyboard accessibility", () => {
    it("should have an accessible label on the search input", () => {
      render(NlqSearchBar, {
        props: { onSearch: vi.fn(), onClear: vi.fn() },
      });

      const input = screen.getByRole("textbox");
      // Either an aria-label or associated label element must exist
      const hasLabel =
        input.hasAttribute("aria-label") ||
        input.hasAttribute("aria-labelledby") ||
        screen.queryByText(/search|query|ask/i) !== null;
      expect(hasLabel).toBe(true);
    });

    it("should focus the input on initial render (or be focusable)", () => {
      render(NlqSearchBar, {
        props: { onSearch: vi.fn(), onClear: vi.fn() },
      });

      const input = screen.getByRole("textbox");
      expect(input).not.toBeDisabled();
      // Verify it is focusable
      input.focus();
      expect(document.activeElement).toBe(input);
    });
  });

  // ---------------------------------------------------------------------------
  // Table-driven: prop combinations
  // ---------------------------------------------------------------------------

  describe("table-driven prop combinations", () => {
    const propCases = [
      {
        description: "default props: no loading, no error, not in search mode",
        props: { loading: false, error: undefined, inSearchMode: false },
        expectLoadingIndicator: false,
        expectAlert: false,
        expectClearButton: false,
      },
      {
        description: "loading with no error",
        props: { loading: true, error: undefined, inSearchMode: false },
        expectLoadingIndicator: true,
        expectAlert: false,
        expectClearButton: false,
      },
      {
        description: "error with no loading",
        props: {
          loading: false,
          error: "Something went wrong",
          inSearchMode: false,
        },
        expectLoadingIndicator: false,
        expectAlert: true,
        expectClearButton: false,
      },
      {
        description: "search mode active",
        props: { loading: false, error: undefined, inSearchMode: true },
        expectLoadingIndicator: false,
        expectAlert: false,
        expectClearButton: true,
      },
      {
        description: "loading + error + search mode",
        props: {
          loading: true,
          error: "Failed",
          inSearchMode: true,
        },
        expectLoadingIndicator: true,
        expectAlert: true,
        expectClearButton: true,
      },
    ];

    it.each(propCases)(
      "$description",
      ({ props, expectLoadingIndicator, expectAlert, expectClearButton }) => {
        render(NlqSearchBar, {
          props: {
            onSearch: vi.fn(),
            onClear: vi.fn(),
            ...props,
          },
        });

        if (expectLoadingIndicator) {
          expect(
            screen.getByTestId("nlq-loading-indicator"),
          ).toBeInTheDocument();
        } else {
          expect(
            screen.queryByTestId("nlq-loading-indicator"),
          ).not.toBeInTheDocument();
        }

        if (expectAlert) {
          expect(screen.getByRole("alert")).toBeInTheDocument();
        } else {
          expect(screen.queryByRole("alert")).not.toBeInTheDocument();
        }

        if (expectClearButton) {
          expect(
            screen.getByRole("button", { name: /back to browse/i }),
          ).toBeInTheDocument();
        } else {
          expect(
            screen.queryByRole("button", { name: /back to browse/i }),
          ).not.toBeInTheDocument();
        }
      },
    );
  });
});
