// Code generated by Tester Agent. DO NOT EDIT.
// Task: chat-redesign-phase-6 — Debug and Health Tools
// Builder must make these tests pass without modification.
//
// NOTE: FlowStatusCard.svelte does not yet exist.
// All tests here will fail until the Builder creates that component.
//
// Expected FlowStatusAttachment shape (must be added to $lib/types/chat.ts):
//   kind: "flow-status"
//   flowId: string
//   flowName: string
//   state: string
//   nodeCount: number
//   connectionCount: number
//   warnings?: string[]
//
// Component props:
//   attachment: FlowStatusAttachment
//   onViewFlow?: (flowId: string) => void
//
// Builder must:
//   1. Add FlowStatusAttachment to the MessageAttachment union in types/chat.ts
//   2. Create FlowStatusCard.svelte with data-testid="flow-status-card"
//   3. Show flowName (clickable, calls onViewFlow), state, nodeCount,
//      connectionCount, and optional warnings list

import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/svelte";
import userEvent from "@testing-library/user-event";
import FlowStatusCard from "./FlowStatusCard.svelte";

// ---------------------------------------------------------------------------
// FlowStatusAttachment fixture (inline to avoid importing not-yet-existing type)
// ---------------------------------------------------------------------------

interface FlowStatusAttachment {
  kind: "flow-status";
  flowId: string;
  flowName: string;
  state: string;
  nodeCount: number;
  connectionCount: number;
  warnings?: string[];
}

function makeAttachment(
  overrides: Partial<FlowStatusAttachment> = {},
): FlowStatusAttachment {
  return {
    kind: "flow-status",
    flowId: "flow-abc-123",
    flowName: "Sensor Ingestion Pipeline",
    state: "running",
    nodeCount: 4,
    connectionCount: 3,
    ...overrides,
  };
}

function makeMinimalAttachment(): FlowStatusAttachment {
  return {
    kind: "flow-status",
    flowId: "flow-bare",
    flowName: "Bare Flow",
    state: "stopped",
    nodeCount: 0,
    connectionCount: 0,
  };
}

// ---------------------------------------------------------------------------
// Rendering — root element and testid
// ---------------------------------------------------------------------------

describe("FlowStatusCard — root element", () => {
  it("renders with data-testid='flow-status-card'", () => {
    render(FlowStatusCard, { props: { attachment: makeAttachment() } });

    expect(screen.getByTestId("flow-status-card")).toBeInTheDocument();
  });

  it("renders without throwing for minimal attachment", () => {
    expect(() =>
      render(FlowStatusCard, {
        props: { attachment: makeMinimalAttachment() },
      }),
    ).not.toThrow();
  });
});

// ---------------------------------------------------------------------------
// Rendering — flow name
// ---------------------------------------------------------------------------

describe("FlowStatusCard — shows flow name", () => {
  it("displays the flowName", () => {
    render(FlowStatusCard, {
      props: {
        attachment: makeAttachment({ flowName: "Maritime Tracking" }),
      },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(
      /Maritime Tracking/i,
    );
  });

  it("displays different flow names correctly", () => {
    render(FlowStatusCard, {
      props: {
        attachment: makeAttachment({ flowName: "Border Sensor Pipeline" }),
      },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(
      /Border Sensor Pipeline/i,
    );
  });
});

// ---------------------------------------------------------------------------
// Rendering — flow state
// ---------------------------------------------------------------------------

describe("FlowStatusCard — shows flow state", () => {
  it("displays 'running' state", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ state: "running" }) },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(/running/i);
  });

  it("displays 'stopped' state", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ state: "stopped" }) },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(/stopped/i);
  });

  it("displays 'error' state", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ state: "error" }) },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(/error/i);
  });

  it("displays 'deploying' state", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ state: "deploying" }) },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(
      /deploying/i,
    );
  });

  it("displays 'not_deployed' state", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ state: "not_deployed" }) },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(
      /not.deployed/i,
    );
  });
});

// ---------------------------------------------------------------------------
// Rendering — node count and connection count
// ---------------------------------------------------------------------------

describe("FlowStatusCard — shows node count and connection count", () => {
  it("displays the nodeCount", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ nodeCount: 7 }) },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(/7/);
  });

  it("displays the connectionCount", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ connectionCount: 5 }) },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(/5/);
  });

  it("displays both nodeCount and connectionCount on the same card", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ nodeCount: 3, connectionCount: 2 }) },
    });

    const card = screen.getByTestId("flow-status-card");
    expect(card).toHaveTextContent(/3/);
    expect(card).toHaveTextContent(/2/);
  });

  it("handles zero nodeCount", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ nodeCount: 0 }) },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(/0/);
  });

  it("handles zero connectionCount", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ connectionCount: 0 }) },
    });

    expect(screen.getByTestId("flow-status-card")).toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Rendering — warnings (optional)
// ---------------------------------------------------------------------------

describe("FlowStatusCard — shows warnings when present", () => {
  it("displays each warning text", () => {
    render(FlowStatusCard, {
      props: {
        attachment: makeAttachment({
          warnings: ["Component 'input' has high error rate"],
        }),
      },
    });

    expect(screen.getByTestId("flow-status-card")).toHaveTextContent(
      /Component 'input' has high error rate/i,
    );
  });

  it("displays multiple warnings", () => {
    render(FlowStatusCard, {
      props: {
        attachment: makeAttachment({
          warnings: [
            "Throughput below threshold",
            "Latency spike detected",
          ],
        }),
      },
    });

    const card = screen.getByTestId("flow-status-card");
    expect(card).toHaveTextContent(/Throughput below threshold/i);
    expect(card).toHaveTextContent(/Latency spike detected/i);
  });

  it("renders without error when warnings are absent", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ warnings: undefined }) },
    });

    expect(screen.getByTestId("flow-status-card")).toBeInTheDocument();
  });

  it("renders without error when warnings array is empty", () => {
    render(FlowStatusCard, {
      props: { attachment: makeAttachment({ warnings: [] }) },
    });

    expect(screen.getByTestId("flow-status-card")).toBeInTheDocument();
  });

  it("does not render a warnings section when warnings are absent", () => {
    render(FlowStatusCard, {
      props: { attachment: makeMinimalAttachment() },
    });

    const card = screen.getByTestId("flow-status-card");
    // No warning-related text should be visible
    expect(card).not.toHaveTextContent(/warning/i);
  });
});

// ---------------------------------------------------------------------------
// Interaction — clicking flow name calls onViewFlow
// ---------------------------------------------------------------------------

describe("FlowStatusCard — clicking flow name calls onViewFlow", () => {
  it("clicking the flow name triggers onViewFlow with flowId", async () => {
    const onViewFlow = vi.fn();
    const user = userEvent.setup();

    render(FlowStatusCard, {
      props: {
        attachment: makeAttachment({ flowId: "flow-click-123" }),
        onViewFlow,
      },
    });

    const flowNameEl = screen.getByTestId("flow-status-flow-name");
    await user.click(flowNameEl);

    expect(onViewFlow).toHaveBeenCalledOnce();
    expect(onViewFlow).toHaveBeenCalledWith("flow-click-123");
  });

  it("calls onViewFlow with correct id for different flows", async () => {
    const onViewFlow = vi.fn();
    const user = userEvent.setup();

    render(FlowStatusCard, {
      props: {
        attachment: makeAttachment({ flowId: "flow-other-456" }),
        onViewFlow,
      },
    });

    await user.click(screen.getByTestId("flow-status-flow-name"));

    expect(onViewFlow).toHaveBeenCalledWith("flow-other-456");
  });
});

// ---------------------------------------------------------------------------
// Interaction — onViewFlow prop is optional
// ---------------------------------------------------------------------------

describe("FlowStatusCard — onViewFlow is optional", () => {
  it("does not throw when onViewFlow is not provided and flow name is clicked", async () => {
    const user = userEvent.setup();

    render(FlowStatusCard, {
      props: { attachment: makeAttachment() },
    });

    await expect(
      user.click(screen.getByTestId("flow-status-flow-name")),
    ).resolves.not.toThrow();
  });
});

// ---------------------------------------------------------------------------
// Table-driven: state × node count combinations
// ---------------------------------------------------------------------------

describe("FlowStatusCard — table-driven state × count display", () => {
  it.each([
    { state: "running", nodeCount: 0, connectionCount: 0 },
    { state: "running", nodeCount: 3, connectionCount: 2 },
    { state: "stopped", nodeCount: 5, connectionCount: 4 },
    { state: "error", nodeCount: 2, connectionCount: 1 },
  ])(
    "state='$state' with $nodeCount nodes and $connectionCount connections renders correctly",
    ({ state, nodeCount, connectionCount }) => {
      render(FlowStatusCard, {
        props: {
          attachment: makeAttachment({ state, nodeCount, connectionCount }),
        },
      });

      const card = screen.getByTestId("flow-status-card");
      expect(card).toHaveTextContent(new RegExp(state, "i"));
      expect(card).toHaveTextContent(new RegExp(String(nodeCount)));
    },
  );
});
