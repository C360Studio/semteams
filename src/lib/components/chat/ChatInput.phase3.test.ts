// Code generated by Tester Agent. DO NOT EDIT.
// Task: chat-redesign-phase-3 — ChatInput chip and slash command integration tests
// Builder must make these tests pass without modification.

import { describe, it, expect, vi } from "vitest";
import { render, screen, waitFor } from "@testing-library/svelte";
import userEvent from "@testing-library/user-event";
import ChatInput from "./ChatInput.svelte";
import type { ContextChip } from "$lib/types/chat";
import type { SlashCommand } from "$lib/types/slashCommand";

// ---------------------------------------------------------------------------
// Fixtures
// ---------------------------------------------------------------------------

function makeChip(overrides: Partial<ContextChip> = {}): ContextChip {
  return {
    id: "chip-1",
    kind: "entity",
    label: "MyEntity",
    value: "org.platform.domain.system.type.instance",
    ...overrides,
  };
}

function makeCommand(
  name: string,
  description = "A command",
  aliases: string[] = [],
): SlashCommand {
  return {
    name,
    aliases,
    description,
    usage: `/${name} [args]`,
    intent: "general",
    availableOn: ["flow-builder", "data-view"],
    parse: (args: string) => ({ intent: "general", content: args, params: {} }),
  };
}

const searchCommand = makeCommand("search", "Search the knowledge graph", [
  "s",
  "find",
]);
const flowCommand = makeCommand("flow", "Create or modify a flow", ["f"]);
const explainCommand = makeCommand("explain", "Explain an entity", ["e"]);

const allCommands = [searchCommand, flowCommand, explainCommand];

// ---------------------------------------------------------------------------
// Context chip bar integration — presence
// ---------------------------------------------------------------------------

describe("ChatInput — context chip bar rendering", () => {
  it("renders ContextChipBar when chips are provided", () => {
    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        chips: [makeChip()],
        onRemoveChip: vi.fn(),
        onClearChips: vi.fn(),
      },
    });

    expect(screen.getByTestId("context-chip-bar")).toBeInTheDocument();
  });

  it("does NOT render ContextChipBar when chips prop is undefined", () => {
    render(ChatInput, { props: { onSubmit: vi.fn() } });

    expect(screen.queryByTestId("context-chip-bar")).not.toBeInTheDocument();
  });

  it("does NOT render ContextChipBar when chips array is empty", () => {
    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        chips: [],
        onRemoveChip: vi.fn(),
        onClearChips: vi.fn(),
      },
    });

    // Empty chips should not show the bar (bar hides itself when empty)
    expect(screen.queryByTestId("context-chip-bar")).not.toBeInTheDocument();
  });

  it("renders chip labels when chips are provided", () => {
    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        chips: [makeChip({ label: "VisibleLabel" })],
        onRemoveChip: vi.fn(),
        onClearChips: vi.fn(),
      },
    });

    expect(screen.getByText("VisibleLabel")).toBeInTheDocument();
  });

  it("renders multiple chips in the bar", () => {
    const chips = [
      makeChip({ id: "c1", label: "First" }),
      makeChip({ id: "c2", label: "Second" }),
    ];

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        chips,
        onRemoveChip: vi.fn(),
        onClearChips: vi.fn(),
      },
    });

    expect(screen.getByText("First")).toBeInTheDocument();
    expect(screen.getByText("Second")).toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Context chip bar integration — removal pass-through
// ---------------------------------------------------------------------------

describe("ChatInput — chip removal pass-through", () => {
  it("calls onRemoveChip with the correct id when a chip remove button is clicked", async () => {
    const onRemoveChip = vi.fn();
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        chips: [makeChip({ id: "chip-xyz", label: "RemovableChip" })],
        onRemoveChip,
        onClearChips: vi.fn(),
      },
    });

    await user.click(
      screen.getByRole("button", { name: "Remove RemovableChip" }),
    );

    expect(onRemoveChip).toHaveBeenCalledOnce();
    expect(onRemoveChip).toHaveBeenCalledWith("chip-xyz");
  });

  it("calls onClearChips when clear-all is clicked (2+ chips)", async () => {
    const onClearChips = vi.fn();
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        chips: [
          makeChip({ id: "c1", label: "Alpha" }),
          makeChip({ id: "c2", label: "Beta" }),
        ],
        onRemoveChip: vi.fn(),
        onClearChips,
      },
    });

    await user.click(screen.getByTestId("chip-bar-clear-all"));

    expect(onClearChips).toHaveBeenCalledOnce();
  });
});

// ---------------------------------------------------------------------------
// Slash command menu — show/hide
// ---------------------------------------------------------------------------

describe("ChatInput — SlashCommandMenu visibility", () => {
  it("shows SlashCommandMenu when user types '/' as the first character", async () => {
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        commands: allCommands,
      },
    });

    await user.type(screen.getByTestId("chat-input"), "/");

    await waitFor(() => {
      expect(screen.getByTestId("slash-command-menu")).toBeInTheDocument();
    });
  });

  it("does NOT show SlashCommandMenu when commands prop is not provided", async () => {
    const user = userEvent.setup();

    render(ChatInput, { props: { onSubmit: vi.fn() } });

    await user.type(screen.getByTestId("chat-input"), "/");

    // No commands prop — no menu should appear
    expect(screen.queryByTestId("slash-command-menu")).not.toBeInTheDocument();
  });

  it("hides SlashCommandMenu when input does not start with '/'", async () => {
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        commands: allCommands,
      },
    });

    const textarea = screen.getByTestId("chat-input");

    // Open the menu
    await user.type(textarea, "/");
    await waitFor(() => {
      expect(screen.getByTestId("slash-command-menu")).toBeInTheDocument();
    });

    // Type a space — menu should close because input no longer starts with "/"
    // (or clear and type non-slash text)
    await user.clear(textarea);
    await user.type(textarea, "hello");

    await waitFor(() => {
      expect(
        screen.queryByTestId("slash-command-menu"),
      ).not.toBeInTheDocument();
    });
  });

  it("hides SlashCommandMenu when Escape is pressed", async () => {
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        commands: allCommands,
      },
    });

    const textarea = screen.getByTestId("chat-input");

    // Open the menu
    await user.type(textarea, "/");
    await waitFor(() => {
      expect(screen.getByTestId("slash-command-menu")).toBeInTheDocument();
    });

    // Press Escape — menu should disappear
    await user.keyboard("{Escape}");

    await waitFor(() => {
      expect(
        screen.queryByTestId("slash-command-menu"),
      ).not.toBeInTheDocument();
    });
  });

  it("does NOT show SlashCommandMenu when '/' appears mid-sentence (not at start)", async () => {
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        commands: allCommands,
      },
    });

    await user.type(screen.getByTestId("chat-input"), "hello /search");

    // Menu should not appear when "/" is not the leading character
    expect(screen.queryByTestId("slash-command-menu")).not.toBeInTheDocument();
  });

  it("shows SlashCommandMenu filtered by text after '/'", async () => {
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        commands: allCommands,
      },
    });

    await user.type(screen.getByTestId("chat-input"), "/se");

    await waitFor(() => {
      expect(screen.getByTestId("slash-command-menu")).toBeInTheDocument();
      // "search" matches "se", "flow" and "explain" should not appear
      expect(screen.getByText(/\/search/)).toBeInTheDocument();
      expect(screen.queryByText(/\/flow/)).not.toBeInTheDocument();
    });
  });
});

// ---------------------------------------------------------------------------
// Slash command selection
// ---------------------------------------------------------------------------

describe("ChatInput — slash command selection", () => {
  it("selecting a command replaces the input with '/{command.name} '", async () => {
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        commands: allCommands,
      },
    });

    const textarea = screen.getByTestId("chat-input");
    await user.type(textarea, "/");

    await waitFor(() => {
      expect(screen.getByTestId("slash-command-menu")).toBeInTheDocument();
    });

    await user.click(screen.getByText(/\/search/));

    await waitFor(() => {
      expect(textarea).toHaveValue("/search ");
    });
  });

  it("after command selection the SlashCommandMenu is dismissed", async () => {
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        commands: allCommands,
      },
    });

    const textarea = screen.getByTestId("chat-input");
    await user.type(textarea, "/");

    await waitFor(() => {
      expect(screen.getByTestId("slash-command-menu")).toBeInTheDocument();
    });

    await user.click(screen.getByText(/\/search/));

    await waitFor(() => {
      expect(
        screen.queryByTestId("slash-command-menu"),
      ).not.toBeInTheDocument();
    });
  });

  it("calls onCommandSelect with the selected command if prop is provided", async () => {
    const onCommandSelect = vi.fn();
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        commands: allCommands,
        onCommandSelect,
      },
    });

    await user.type(screen.getByTestId("chat-input"), "/");

    await waitFor(() => {
      expect(screen.getByTestId("slash-command-menu")).toBeInTheDocument();
    });

    await user.click(screen.getByText(/\/search/));

    expect(onCommandSelect).toHaveBeenCalledOnce();
    expect(onCommandSelect).toHaveBeenCalledWith(searchCommand);
  });

  it("submitting after slash command selection still calls onSubmit normally", async () => {
    const onSubmit = vi.fn();
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit,
        commands: allCommands,
      },
    });

    const textarea = screen.getByTestId("chat-input");
    await user.type(textarea, "/");

    await waitFor(() => {
      expect(screen.getByTestId("slash-command-menu")).toBeInTheDocument();
    });

    await user.click(screen.getByText(/\/search/));

    // Input now has "/search " — type query and submit
    await user.type(textarea, "kafka topics{Enter}");

    expect(onSubmit).toHaveBeenCalledOnce();
    expect(onSubmit).toHaveBeenCalledWith("/search kafka topics");
  });
});

// ---------------------------------------------------------------------------
// Coexistence — chips + slash commands together
// ---------------------------------------------------------------------------

describe("ChatInput — chips and slash commands coexist", () => {
  it("shows both chip bar and slash menu when chips are provided and '/' is typed", async () => {
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        chips: [makeChip({ label: "AnEntity" })],
        onRemoveChip: vi.fn(),
        onClearChips: vi.fn(),
        commands: allCommands,
      },
    });

    expect(screen.getByTestId("context-chip-bar")).toBeInTheDocument();

    await user.type(screen.getByTestId("chat-input"), "/");

    await waitFor(() => {
      expect(screen.getByTestId("slash-command-menu")).toBeInTheDocument();
    });

    // Chip bar should still be visible
    expect(screen.getByTestId("context-chip-bar")).toBeInTheDocument();
  });

  it("ContextChipBar is positioned above the textarea", () => {
    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        chips: [makeChip()],
        onRemoveChip: vi.fn(),
        onClearChips: vi.fn(),
      },
    });

    const bar = screen.getByTestId("context-chip-bar");
    const textarea = screen.getByTestId("chat-input");

    // Bar should appear before textarea in DOM order
    const position = bar.compareDocumentPosition(textarea);
    // DOCUMENT_POSITION_FOLLOWING = 4 means textarea comes after bar in the DOM
    expect(position & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();
  });
});

// ---------------------------------------------------------------------------
// Existing behavior not broken by Phase 3 additions
// ---------------------------------------------------------------------------

describe("ChatInput — existing submit behavior unaffected by phase-3 props", () => {
  it("still calls onSubmit when Enter is pressed even when chips are present", async () => {
    const onSubmit = vi.fn();
    const user = userEvent.setup();

    render(ChatInput, {
      props: {
        onSubmit,
        chips: [makeChip()],
        onRemoveChip: vi.fn(),
        onClearChips: vi.fn(),
      },
    });

    const textarea = screen.getByTestId("chat-input");
    await user.type(textarea, "my message{Enter}");

    expect(onSubmit).toHaveBeenCalledWith("my message");
  });

  it("still shows cancel button when isStreaming=true alongside chips", () => {
    render(ChatInput, {
      props: {
        onSubmit: vi.fn(),
        isStreaming: true,
        chips: [makeChip()],
        onRemoveChip: vi.fn(),
        onClearChips: vi.fn(),
      },
    });

    expect(screen.getByTestId("chat-cancel")).toBeInTheDocument();
    expect(screen.queryByTestId("chat-submit")).not.toBeInTheDocument();
  });
});
