// Code generated by Tester Agent. DO NOT EDIT.
// Task: chat-redesign-phase-3 — ContextChipPill component tests
// Builder must make these tests pass without modification.

import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/svelte";
import userEvent from "@testing-library/user-event";
import ContextChipPill from "./ContextChipPill.svelte";
import type { ContextChip, ContextChipKind } from "$lib/types/chat";

// ---------------------------------------------------------------------------
// Fixtures
// ---------------------------------------------------------------------------

function makeChip(overrides: Partial<ContextChip> = {}): ContextChip {
  return {
    id: "chip-1",
    kind: "entity",
    label: "MyEntity",
    value: "org.platform.domain.system.type.instance",
    ...overrides,
  };
}

// ---------------------------------------------------------------------------
// Rendering — label
// ---------------------------------------------------------------------------

describe("ContextChipPill — label rendering", () => {
  it("renders the chip label text", () => {
    render(ContextChipPill, {
      props: { chip: makeChip({ label: "SomeEntity" }), onRemove: vi.fn() },
    });

    expect(screen.getByText("SomeEntity")).toBeInTheDocument();
  });

  it("renders a chip with a long label without throwing", () => {
    const longLabel = "A".repeat(200);
    expect(() =>
      render(ContextChipPill, {
        props: { chip: makeChip({ label: longLabel }), onRemove: vi.fn() },
      }),
    ).not.toThrow();

    expect(screen.getByText(longLabel)).toBeInTheDocument();
  });

  it("renders label with special characters correctly", () => {
    render(ContextChipPill, {
      props: {
        chip: makeChip({ label: "Entity <Test> & More" }),
        onRemove: vi.fn(),
      },
    });

    expect(screen.getByText("Entity <Test> & More")).toBeInTheDocument();
  });

  it("renders label with emoji without throwing", () => {
    expect(() =>
      render(ContextChipPill, {
        props: { chip: makeChip({ label: "Entity 🔍" }), onRemove: vi.fn() },
      }),
    ).not.toThrow();
  });
});

// ---------------------------------------------------------------------------
// Rendering — kind indicator
// ---------------------------------------------------------------------------

describe("ContextChipPill — kind indicator", () => {
  it("renders the kind indicator element", () => {
    render(ContextChipPill, {
      props: { chip: makeChip({ kind: "entity" }), onRemove: vi.fn() },
    });

    expect(screen.getByTestId("chip-kind")).toBeInTheDocument();
  });

  it("reflects the correct kind on the kind indicator", () => {
    render(ContextChipPill, {
      props: { chip: makeChip({ kind: "component" }), onRemove: vi.fn() },
    });

    const kindEl = screen.getByTestId("chip-kind");
    // The element should somehow indicate the kind — via text content, data attribute, or aria label
    const indicatesKind =
      kindEl.textContent !== "" ||
      kindEl.getAttribute("data-kind") !== null ||
      kindEl.getAttribute("aria-label") !== null ||
      kindEl.getAttribute("title") !== null;

    expect(indicatesKind).toBe(true);
  });
});

// ---------------------------------------------------------------------------
// Rendering — data-testid
// ---------------------------------------------------------------------------

describe("ContextChipPill — data-testid", () => {
  it("has data-testid='context-chip-{chip.id}' with a standard id", () => {
    render(ContextChipPill, {
      props: { chip: makeChip({ id: "abc-123" }), onRemove: vi.fn() },
    });

    expect(screen.getByTestId("context-chip-abc-123")).toBeInTheDocument();
  });

  it("has the correct data-testid when id contains underscores", () => {
    render(ContextChipPill, {
      props: { chip: makeChip({ id: "entity_chip_1" }), onRemove: vi.fn() },
    });

    expect(
      screen.getByTestId("context-chip-entity_chip_1"),
    ).toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Rendering — remove button
// ---------------------------------------------------------------------------

describe("ContextChipPill — remove button", () => {
  it("renders a remove button", () => {
    render(ContextChipPill, {
      props: { chip: makeChip({ label: "MyEntity" }), onRemove: vi.fn() },
    });

    expect(
      screen.getByRole("button", { name: "Remove MyEntity" }),
    ).toBeInTheDocument();
  });

  it("remove button has aria-label='Remove {label}'", () => {
    render(ContextChipPill, {
      props: { chip: makeChip({ label: "KafkaSource" }), onRemove: vi.fn() },
    });

    const removeBtn = screen.getByRole("button", {
      name: "Remove KafkaSource",
    });
    expect(removeBtn).toHaveAttribute("aria-label", "Remove KafkaSource");
  });

  it("remove button aria-label matches the chip label exactly", () => {
    const label = "My Special Flow";
    render(ContextChipPill, {
      props: { chip: makeChip({ label }), onRemove: vi.fn() },
    });

    expect(
      screen.getByRole("button", { name: `Remove ${label}` }),
    ).toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Interaction — remove callback
// ---------------------------------------------------------------------------

describe("ContextChipPill — onRemove callback", () => {
  it("calls onRemove with chip.id when remove button is clicked", async () => {
    const onRemove = vi.fn();
    const user = userEvent.setup();

    render(ContextChipPill, {
      props: { chip: makeChip({ id: "chip-42" }), onRemove },
    });

    await user.click(screen.getByRole("button", { name: /remove/i }));

    expect(onRemove).toHaveBeenCalledOnce();
    expect(onRemove).toHaveBeenCalledWith("chip-42");
  });

  it("does NOT call onRemove when label is clicked (only remove button triggers it)", async () => {
    const onRemove = vi.fn();
    const user = userEvent.setup();

    render(ContextChipPill, {
      props: {
        chip: makeChip({ id: "chip-99", label: "ClickLabel" }),
        onRemove,
      },
    });

    await user.click(screen.getByText("ClickLabel"));

    expect(onRemove).not.toHaveBeenCalled();
  });

  it("calls onRemove exactly once per click", async () => {
    const onRemove = vi.fn();
    const user = userEvent.setup();

    render(ContextChipPill, {
      props: { chip: makeChip({ id: "chip-1" }), onRemove },
    });

    await user.click(screen.getByRole("button", { name: /remove/i }));

    expect(onRemove).toHaveBeenCalledTimes(1);
  });

  it("calls onRemove with the correct id on multiple distinct chips", async () => {
    const onRemove = vi.fn();
    const user = userEvent.setup();

    const { unmount } = render(ContextChipPill, {
      props: { chip: makeChip({ id: "chip-A", label: "EntityA" }), onRemove },
    });

    await user.click(screen.getByRole("button", { name: "Remove EntityA" }));
    expect(onRemove).toHaveBeenCalledWith("chip-A");
    unmount();
    onRemove.mockClear();

    render(ContextChipPill, {
      props: { chip: makeChip({ id: "chip-B", label: "EntityB" }), onRemove },
    });

    await user.click(screen.getByRole("button", { name: "Remove EntityB" }));
    expect(onRemove).toHaveBeenCalledWith("chip-B");
  });
});

// ---------------------------------------------------------------------------
// Table-driven: all chip kinds
// ---------------------------------------------------------------------------

describe("ContextChipPill — all chip kinds render correctly", () => {
  const kindCases: { kind: ContextChipKind; label: string }[] = [
    { kind: "entity", label: "An Entity" },
    { kind: "component", label: "A Component" },
    { kind: "flow", label: "A Flow" },
    { kind: "community", label: "A Community" },
    { kind: "custom", label: "Custom Item" },
  ];

  it.each(kindCases)(
    "renders kind='$kind' with label='$label'",
    ({ kind, label }) => {
      render(ContextChipPill, {
        props: {
          chip: makeChip({ id: `chip-${kind}`, kind, label }),
          onRemove: vi.fn(),
        },
      });

      expect(
        screen.getByTestId(`context-chip-chip-${kind}`),
      ).toBeInTheDocument();
      expect(screen.getByText(label)).toBeInTheDocument();
      expect(screen.getByTestId("chip-kind")).toBeInTheDocument();
      expect(
        screen.getByRole("button", { name: `Remove ${label}` }),
      ).toBeInTheDocument();
    },
  );
});

// ---------------------------------------------------------------------------
// Accessibility
// ---------------------------------------------------------------------------

describe("ContextChipPill — accessibility", () => {
  it("remove button has type='button'", () => {
    render(ContextChipPill, {
      props: { chip: makeChip(), onRemove: vi.fn() },
    });

    const removeBtn = screen.getByRole("button", { name: /remove/i });
    expect(removeBtn).toHaveAttribute("type", "button");
  });

  it("chip container is present as a DOM element (not hidden)", () => {
    render(ContextChipPill, {
      props: { chip: makeChip({ id: "visible-chip" }), onRemove: vi.fn() },
    });

    const chip = screen.getByTestId("context-chip-visible-chip");
    expect(chip).toBeVisible();
  });
});
