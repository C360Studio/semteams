// Code generated by Tester Agent. DO NOT EDIT.
// Task: chat-redesign-phase-3 — ContextChipBar component tests
// Builder must make these tests pass without modification.

import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/svelte";
import userEvent from "@testing-library/user-event";
import ContextChipBar from "./ContextChipBar.svelte";
import type { ContextChip } from "$lib/types/chat";

// ---------------------------------------------------------------------------
// Fixtures
// ---------------------------------------------------------------------------

function makeChip(overrides: Partial<ContextChip> = {}): ContextChip {
  return {
    id: `chip-${Math.random().toString(36).slice(2)}`,
    kind: "entity",
    label: "EntityLabel",
    value: "some.entity.value",
    ...overrides,
  };
}

const chip1 = makeChip({ id: "chip-1", label: "Alpha" });
const chip2 = makeChip({ id: "chip-2", label: "Beta" });
const chip3 = makeChip({ id: "chip-3", label: "Gamma" });

// ---------------------------------------------------------------------------
// Presence
// ---------------------------------------------------------------------------

describe("ContextChipBar — presence", () => {
  it("has data-testid='context-chip-bar'", () => {
    render(ContextChipBar, {
      props: { chips: [chip1], onRemoveChip: vi.fn(), onClearAll: vi.fn() },
    });

    expect(screen.getByTestId("context-chip-bar")).toBeInTheDocument();
  });

  it("is NOT rendered (hidden or absent) when chips array is empty", () => {
    render(ContextChipBar, {
      props: { chips: [], onRemoveChip: vi.fn(), onClearAll: vi.fn() },
    });

    // Either not in DOM, or hidden
    const bar = screen.queryByTestId("context-chip-bar");
    const isAbsent = bar === null;
    const isHidden =
      bar !== null && !bar.offsetParent && bar.getAttribute("hidden") !== null;
    // Accept either: element absent, or element present but not visible
    expect(isAbsent || isHidden).toBe(true);
  });
});

// ---------------------------------------------------------------------------
// Chip rendering
// ---------------------------------------------------------------------------

describe("ContextChipBar — chip rendering", () => {
  it("renders a pill for each chip", () => {
    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2, chip3],
        onRemoveChip: vi.fn(),
        onClearAll: vi.fn(),
      },
    });

    expect(screen.getByTestId("context-chip-chip-1")).toBeInTheDocument();
    expect(screen.getByTestId("context-chip-chip-2")).toBeInTheDocument();
    expect(screen.getByTestId("context-chip-chip-3")).toBeInTheDocument();
  });

  it("renders the chip label text for each chip", () => {
    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2],
        onRemoveChip: vi.fn(),
        onClearAll: vi.fn(),
      },
    });

    expect(screen.getByText("Alpha")).toBeInTheDocument();
    expect(screen.getByText("Beta")).toBeInTheDocument();
  });

  it("renders exactly the correct number of chips", () => {
    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2, chip3],
        onRemoveChip: vi.fn(),
        onClearAll: vi.fn(),
      },
    });

    // Each chip has a remove button with aria-label="Remove ..."
    const removeBtns = screen.getAllByRole("button", { name: /^remove /i });
    expect(removeBtns).toHaveLength(3);
  });

  it("renders a single chip correctly", () => {
    render(ContextChipBar, {
      props: {
        chips: [chip1],
        onRemoveChip: vi.fn(),
        onClearAll: vi.fn(),
      },
    });

    expect(screen.getByText("Alpha")).toBeInTheDocument();
    expect(screen.getByTestId("context-chip-chip-1")).toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Chip count indicator
// ---------------------------------------------------------------------------

describe("ContextChipBar — chip count indicator", () => {
  it("shows a chip count when chips are present", () => {
    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2, chip3],
        onRemoveChip: vi.fn(),
        onClearAll: vi.fn(),
      },
    });

    const bar = screen.getByTestId("context-chip-bar");
    // Count "3" should appear somewhere in the bar (as text or attribute)
    expect(bar.textContent).toMatch(/3/);
  });

  it("shows chip count '1' when only one chip is present", () => {
    render(ContextChipBar, {
      props: {
        chips: [chip1],
        onRemoveChip: vi.fn(),
        onClearAll: vi.fn(),
      },
    });

    const bar = screen.getByTestId("context-chip-bar");
    expect(bar.textContent).toMatch(/1/);
  });
});

// ---------------------------------------------------------------------------
// Clear all button visibility
// ---------------------------------------------------------------------------

describe("ContextChipBar — clear-all button visibility", () => {
  it("shows a 'Clear all' button when chips.length > 1", () => {
    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2],
        onRemoveChip: vi.fn(),
        onClearAll: vi.fn(),
      },
    });

    expect(screen.getByTestId("chip-bar-clear-all")).toBeInTheDocument();
  });

  it("shows 'Clear all' button for 3 chips", () => {
    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2, chip3],
        onRemoveChip: vi.fn(),
        onClearAll: vi.fn(),
      },
    });

    expect(screen.getByTestId("chip-bar-clear-all")).toBeInTheDocument();
  });

  it("does NOT show 'Clear all' button when only 1 chip is present", () => {
    render(ContextChipBar, {
      props: {
        chips: [chip1],
        onRemoveChip: vi.fn(),
        onClearAll: vi.fn(),
      },
    });

    expect(screen.queryByTestId("chip-bar-clear-all")).not.toBeInTheDocument();
  });

  it("does NOT show 'Clear all' button when chips array is empty", () => {
    render(ContextChipBar, {
      props: { chips: [], onRemoveChip: vi.fn(), onClearAll: vi.fn() },
    });

    expect(screen.queryByTestId("chip-bar-clear-all")).not.toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Interaction — clear all
// ---------------------------------------------------------------------------

describe("ContextChipBar — onClearAll callback", () => {
  it("calls onClearAll when 'Clear all' button is clicked", async () => {
    const onClearAll = vi.fn();
    const user = userEvent.setup();

    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2],
        onRemoveChip: vi.fn(),
        onClearAll,
      },
    });

    await user.click(screen.getByTestId("chip-bar-clear-all"));

    expect(onClearAll).toHaveBeenCalledOnce();
  });

  it("calls onClearAll exactly once per click", async () => {
    const onClearAll = vi.fn();
    const user = userEvent.setup();

    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2, chip3],
        onRemoveChip: vi.fn(),
        onClearAll,
      },
    });

    await user.click(screen.getByTestId("chip-bar-clear-all"));

    expect(onClearAll).toHaveBeenCalledTimes(1);
  });

  it("does NOT call onRemoveChip when clear-all is clicked", async () => {
    const onRemoveChip = vi.fn();
    const user = userEvent.setup();

    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2],
        onRemoveChip,
        onClearAll: vi.fn(),
      },
    });

    await user.click(screen.getByTestId("chip-bar-clear-all"));

    expect(onRemoveChip).not.toHaveBeenCalled();
  });
});

// ---------------------------------------------------------------------------
// Interaction — individual chip removal
// ---------------------------------------------------------------------------

describe("ContextChipBar — onRemoveChip callback", () => {
  it("calls onRemoveChip with the correct chip id when remove is clicked", async () => {
    const onRemoveChip = vi.fn();
    const user = userEvent.setup();

    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2],
        onRemoveChip,
        onClearAll: vi.fn(),
      },
    });

    await user.click(screen.getByRole("button", { name: "Remove Alpha" }));

    expect(onRemoveChip).toHaveBeenCalledOnce();
    expect(onRemoveChip).toHaveBeenCalledWith("chip-1");
  });

  it("calls onRemoveChip with the second chip's id when second remove is clicked", async () => {
    const onRemoveChip = vi.fn();
    const user = userEvent.setup();

    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2],
        onRemoveChip,
        onClearAll: vi.fn(),
      },
    });

    await user.click(screen.getByRole("button", { name: "Remove Beta" }));

    expect(onRemoveChip).toHaveBeenCalledWith("chip-2");
  });

  it("does NOT call onClearAll when individual chip remove is clicked", async () => {
    const onClearAll = vi.fn();
    const user = userEvent.setup();

    render(ContextChipBar, {
      props: {
        chips: [chip1, chip2],
        onRemoveChip: vi.fn(),
        onClearAll,
      },
    });

    await user.click(screen.getByRole("button", { name: "Remove Alpha" }));

    expect(onClearAll).not.toHaveBeenCalled();
  });
});

// ---------------------------------------------------------------------------
// Table-driven: chip count visibility rules
// ---------------------------------------------------------------------------

describe("ContextChipBar — clear-all visibility by count", () => {
  const cases = [
    { count: 0, showClearAll: false },
    { count: 1, showClearAll: false },
    { count: 2, showClearAll: true },
    { count: 5, showClearAll: true },
    { count: 10, showClearAll: true },
  ];

  it.each(cases)(
    "shows clear-all=$showClearAll when count=$count",
    ({ count, showClearAll }) => {
      const chips = Array.from({ length: count }, (_, i) =>
        makeChip({ id: `chip-${i}`, label: `Label ${i}` }),
      );

      render(ContextChipBar, {
        props: { chips, onRemoveChip: vi.fn(), onClearAll: vi.fn() },
      });

      if (showClearAll) {
        expect(screen.getByTestId("chip-bar-clear-all")).toBeInTheDocument();
      } else {
        expect(
          screen.queryByTestId("chip-bar-clear-all"),
        ).not.toBeInTheDocument();
      }
    },
  );
});
