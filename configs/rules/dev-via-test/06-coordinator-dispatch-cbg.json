{
  "id": "dev_via_test_coordinator_dispatch_cbg",
  "type": "expression",
  "name": "Walker decide(dev_via_test_finalize) → CBG (reviewer-dev-via-test)",
  "description": "ADR-044 Slice 4 — walker-finalize path. Fires when the dev-via-test walker (a coordinator loop) terminates with decide(action=dev_via_test_finalize) — all per-task Ralphs have completed (or been routed through ask_user) and the chain is ready for CBG's chain-end gate. Spawns reviewer-dev-via-test (CBG) to run plan.integration_test_command across all task scope, diff against plan.chain_start_git_tag, and emit decide(approved) or decide(rejected). The dev_via_test_finalize token is a NEW closed-taxonomy action introduced in Slice 4 (per ADR-044 §addendum Slice 4) — distinct from dev_via_test (which routes to Lisa initial OR Ralph walker dispatch via rule 01/03's subtopics.length differentiator) to avoid overloading a third meaning on the same token.",
  "enabled": true,
  "conditions": [
    {
      "field": "agent.loop.role",
      "operator": "eq",
      "value": "coordinator",
      "required": true
    },
    {
      "field": "coordinator.decision.next_action",
      "operator": "eq",
      "value": "dev_via_test_finalize",
      "required": true
    }
  ],
  "logic": "and",
  "on_enter": [
    {
      "type": "publish_agent",
      "subject": "agent.task.reviewer-dev-via-test",
      "role": "reviewer-dev-via-test",
      "model": "reviewing",
      "tools": ["read_loop_result", "query_entity", "bash", "scratchpad", "decide"],
      "tool_choice": { "mode": "required" },
      "action_allowlist": ["approved", "rejected"],
      "properties": {
        "run_loop_id": "$entity.triple.lineage.run-loop-entity-id",
        "category": "dev_via_test",
        "phase": "review",
        "walker_loop_id": "$entity.instance"
      },
      "related_loops": {
        "dev-via-test-run": "$entity.triple.lineage.dev-via-test-run",
        "run-loop-entity-id": "$entity.triple.lineage.run-loop-entity-id",
        "finalizing-walker-loop-id": "$entity.instance"
      },
      "prompt": "DEV-VIA-TEST CBG — chain-end gate.\n\nThe walker at loop $entity.instance has declared all tasks complete and dispatched you to verify. Your job: run the plan's integration test command across the cumulative work, sanity-check the diff against the chain-start tag, and emit decide(approved | rejected).\n\nYour run entity ID is $entity.triple.lineage.run-loop-entity-id. Call query_entity(entity_id=\"$entity.triple.lineage.run-loop-entity-id\") FIRST to read:\n\n- plan.goal — what the chain set out to do\n- plan.integration_test_command — the FULL acceptance suite (your primary gate)\n- plan.chain_start_git_tag — the git tag Lisa created at chain start (e.g. \"plan-start\"). You'll need this exact value for the diff step.\n- plan.task.* — the per-task scope (what each Ralph was supposed to deliver)\n- dev_via_test.execute.task_completed / .task_failed — which tasks finished cleanly vs blocked (multi-valued)\n\nRead the walker's terminal too: read_loop_result(loop_id=\"$entity.instance\") returns the walker's decide reason — their rollup summary.\n\nYour iteration loop:\n\n1. **Run the integration test command** via bash. Use the EXACT command from plan.integration_test_command (read in the query_entity step above). Capture stdout + stderr + exit code. This is the deterministic cross-task-drift catcher per ADR-044 §CBG's gate at chain-end — even if every per-task test passed, the integration suite catches contamination between tasks.\n\n2. **Read the cumulative diff** via bash. Use the tag value from plan.chain_start_git_tag (read in the query_entity step above) — substitute it into the bash command yourself; the rule layer does NOT pre-resolve plan triples into your prompt:\n     bash git diff <plan.chain_start_git_tag-value-from-query-entity>\n   Skim it — is the work what the plan asked for? Are there changes WAY outside the plan's stated target_files (e.g., unrelated files modified)? File-scope violations are a reason to reject even if the integration test passes.\n\n3. **Decide:**\n   - Integration test passes AND diff sane → decide(action=\"approved\", reason=\"<2-4 sentence rollup naming the user-facing capability shipped, the integration command result, and any noteworthy observations from the diff>\"). The coordinator's final wake-up uses your reason as the user-facing answer.\n   - Integration test fails OR diff shows obvious test-gaming (e.g., tests disabled, assertions removed) OR file-scope blown out (changes far outside any task's target_files) → decide(action=\"rejected\", reason=\"<plain-English explanation of what went wrong; quote the failure output; cite the rule that broke>\"). The coordinator routes your reason through ask_user.\n\nYou are NOT here to iterate on the work. If the integration test fails, you reject — you don't fix it yourself. Fixing is Ralph's job, plan-amendment is the user's call. Per ADR-044 §CBG's gate at chain-end: 'Per-Ralph reviewer is optional in v1 (test-pass is the deterministic signal). Don't slip into per-Ralph reviewer ceremony — that's the BMAD pattern that this ADR exists to avoid.' Same posture for CBG: one gate, one verdict, no recovery loop."
    }
  ],
  "on_exit": [],
  "metadata": {
    "pack": "dev_via_test",
    "phase": "ADR-044 Slice 4 — CBG dispatch",
    "adr": "docs/adr/044-dev-via-test-pack.md",
    "new_action_token": "dev_via_test_finalize — added to coordinator's closed taxonomy in Slice 4. Distinguishes 'all tasks done, finalize chain' from 'walker dispatching next task' (dev_via_test + subtopics) and 'walker responding directly to user' (respond_direct, used for early-exit / Ralph-stuck routing).",
    "rationale_vs_overloading_dev_via_test": "Considered: reuse dev_via_test action with sentinel subtopics like [\"__cbg__\"] or empty subtopics + lineage check. Rejected: rule 03's for_each over subtopics would dispatch Ralph at task id '__cbg__'; empty subtopics from walker with lineage would silently no-op (rule 01's lineage fence excludes walker; no rule fires). New action token is the cleanest dispatch contract — no rule-condition gymnastics, no sentinel-string fragility."
  }
}
