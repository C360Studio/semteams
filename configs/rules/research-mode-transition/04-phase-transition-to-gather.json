{
  "id": "researcher_phase_transition_to_gather",
  "type": "expression",
  "name": "Researcher Phase Transition → GATHER",
  "description": "ADR-041 Slice 2D-3 forward/back-edge spawn rule. Fires when phasevalidator.PhaseValidator has approved a transition to the gather phase (any allowed source — plan forward, synthesize back-edge, architect back-edge) and stamped chain.phase_transition.proceed.gather=\"true\" on the source researcher loop entity. The validator has already checked the edge against the allowed-set AND the per-phase cap (max 3 gather fires per chain); rule fires only when both gates pass. Two-step fire shape mirrors the recoverycounter→rule_02 pattern: triggering loop's terminal triples land first, validator wakes on the same agent.complete and writes the proceed sentinel, the loop's entity_watcher re-evaluates, this rule's condition now passes, fires.",
  "enabled": true,
  "conditions": [
    {
      "field": "chain.phase_transition.proceed.gather",
      "operator": "eq",
      "value": "true",
      "required": true
    }
  ],
  "logic": "and",
  "on_enter": [
    {
      "type": "publish_agent",
      "subject": "agent.task.researcher-gather",
      "role": "researcher-gather",
      "model": "research",
      "tools": ["read_loop_result", "query_entity", "query_entities", "scratchpad", "decide"],
      "action_allowlist": ["synthesize", "needs_clarification"],
      "properties": {
        "prior_loop_id": "$entity.instance",
        "phase": "gather"
      },
      "prompt": "GATHER phase entry. Source loop $entity.instance has handed off with decide(action=\"gather\"). Call read_loop_result on that loop to read the upstream phase's narrative terminal (plan's scope + verifiable outcomes, or synthesize/architect's back-edge reason naming a corpus gap). Iterate query_entity / query_entities + scratchpad evidence-collection per researcher-gather/10-iteration-rules.md. Terminate with decide(action=\"synthesize\", reason=\"<one-sentence summary of evidence accumulated>\") when the upstream questions are covered, OR decide(action=\"needs_clarification\", reason=\"<why the plan's question is unanswerable>\") when the corpus structurally cannot resolve a plan-required question."
    }
  ],
  "on_exit": [],
  "metadata": {
    "phase": "ADR-041 Slice 2D-3",
    "adr": "docs/adr/041-mvp-role-compression-and-graph-as-substrate.md",
    "validator": "cmd/semteams/phasevalidator/phase.go (PhaseValidator)",
    "proceed_sentinel": "chain.phase_transition.proceed.gather on source researcher loop entity. Absence-of-sentinel blocks the rule fire fail-safe.",
    "phase_cap": "Per-phase cap = 3 (ADR-041 §Per-phase cap). Validator enforces; this rule trusts the sentinel."
  }
}
