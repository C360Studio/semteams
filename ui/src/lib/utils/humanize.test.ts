// Code generated by Tester Agent. DO NOT EDIT.
// Task: PropertiesPanel UX Improvements - Humanize Field Names
// Builder must make these tests pass without modification.

import { describe, it, expect } from "vitest";
import { humanizeFieldName } from "./humanize";

/**
 * Unit tests for humanizeFieldName utility function.
 * Converts snake_case field names to Title Case for better readability.
 */
describe("humanizeFieldName", () => {
  describe("basic conversions", () => {
    it("should convert simple snake_case to Title Case", () => {
      expect(humanizeFieldName("bind_address")).toBe("Bind Address");
    });

    it("should convert single word to Title Case", () => {
      expect(humanizeFieldName("port")).toBe("Port");
    });

    it("should convert multiple underscores", () => {
      expect(humanizeFieldName("max_buffer_size")).toBe("Max Buffer Size");
    });

    it("should handle already capitalized words", () => {
      expect(humanizeFieldName("UDP_PORT")).toBe("UDP Port");
    });
  });

  describe("acronym handling", () => {
    it("should keep IP uppercase", () => {
      expect(humanizeFieldName("ip_address")).toBe("IP Address");
    });

    it("should keep UDP uppercase", () => {
      expect(humanizeFieldName("udp_port")).toBe("UDP Port");
    });

    it("should keep TCP uppercase", () => {
      expect(humanizeFieldName("tcp_host")).toBe("TCP Host");
    });

    it("should keep HTTP uppercase", () => {
      expect(humanizeFieldName("http_timeout")).toBe("HTTP Timeout");
    });

    it("should keep HTTPS uppercase", () => {
      expect(humanizeFieldName("https_enabled")).toBe("HTTPS Enabled");
    });

    it("should keep URL uppercase", () => {
      expect(humanizeFieldName("base_url")).toBe("Base URL");
    });

    it("should keep API uppercase", () => {
      expect(humanizeFieldName("api_key")).toBe("API Key");
    });

    it("should keep TTL uppercase", () => {
      expect(humanizeFieldName("cache_ttl")).toBe("Cache TTL");
    });

    it("should keep ID uppercase", () => {
      expect(humanizeFieldName("client_id")).toBe("Client ID");
    });

    it("should handle multiple acronyms", () => {
      expect(humanizeFieldName("tcp_ip_address")).toBe("TCP IP Address");
    });
  });

  describe("edge cases", () => {
    it("should handle empty string", () => {
      expect(humanizeFieldName("")).toBe("");
    });

    it("should handle single character", () => {
      expect(humanizeFieldName("x")).toBe("X");
    });

    it("should handle leading underscore", () => {
      expect(humanizeFieldName("_private")).toBe("Private");
    });

    it("should handle trailing underscore", () => {
      expect(humanizeFieldName("field_")).toBe("Field");
    });

    it("should handle multiple consecutive underscores", () => {
      expect(humanizeFieldName("some__field")).toBe("Some Field");
    });

    it("should handle numbers in field name", () => {
      expect(humanizeFieldName("ipv4_address")).toBe("IPv4 Address");
    });

    it("should handle mixed case input", () => {
      expect(humanizeFieldName("MixedCase_Field")).toBe("Mixed Case Field");
    });
  });

  describe("real-world examples", () => {
    it("should humanize common component config fields", () => {
      const testCases = [
        { input: "bind_address", expected: "Bind Address" },
        { input: "buffer_size", expected: "Buffer Size" },
        { input: "max_connections", expected: "Max Connections" },
        { input: "enable_compression", expected: "Enable Compression" },
        { input: "timeout_seconds", expected: "Timeout Seconds" },
        { input: "retry_attempts", expected: "Retry Attempts" },
        { input: "log_level", expected: "Log Level" },
        { input: "nats_url", expected: "NATS URL" },
        { input: "ws_endpoint", expected: "WS Endpoint" },
      ];

      testCases.forEach(({ input, expected }) => {
        expect(humanizeFieldName(input)).toBe(expected);
      });
    });
  });
});
