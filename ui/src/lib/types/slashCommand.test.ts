// Code generated by Tester Agent. DO NOT EDIT.
// Task: chat-redesign-phase-2 — slash commands and tool registry
// Builder must make these tests pass without modification.

import { describe, it, expect } from "vitest";
import type {
  SlashCommand,
  SlashCommandResult,
  SlashCommandMatch,
  PageKind,
} from "./slashCommand";
import type { ChatIntent } from "$lib/types/chat";

// ---------------------------------------------------------------------------
// Type shape validation
// ---------------------------------------------------------------------------

describe("SlashCommand type shape", () => {
  it("accepts a fully-specified SlashCommand object", () => {
    const cmd: SlashCommand = {
      name: "search",
      aliases: ["s", "find"],
      description: "Search the knowledge graph",
      usage: "/search <query>",
      intent: "search",
      availableOn: ["flow-builder", "data-view"],
      parse: (args: string) => ({
        intent: "search",
        content: args,
        params: { query: args },
      }),
    };

    expect(cmd.name).toBe("search");
    expect(cmd.aliases).toContain("find");
    expect(cmd.intent).toBe("search");
    expect(cmd.availableOn).toContain("flow-builder");
    expect(cmd.availableOn).toContain("data-view");
    expect(typeof cmd.parse).toBe("function");
  });

  it("SlashCommandResult has intent, content, and params", () => {
    const result: SlashCommandResult = {
      intent: "search",
      content: "drones in sector 7",
      params: { query: "drones in sector 7" },
    };

    expect(result.intent).toBe("search");
    expect(result.content).toBe("drones in sector 7");
    expect(result.params).toBeDefined();
  });

  it("SlashCommandMatch pairs a command with its parsed result", () => {
    const cmd: SlashCommand = {
      name: "health",
      aliases: ["h", "status"],
      description: "Show system health",
      usage: "/health",
      intent: "health",
      availableOn: ["flow-builder", "data-view"],
      parse: (_args: string) => ({
        intent: "health",
        content: "Show system health",
        params: {},
      }),
    };

    const match: SlashCommandMatch = {
      command: cmd,
      result: cmd.parse(""),
    };

    expect(match.command.name).toBe("health");
    expect(match.result.intent).toBe("health");
  });

  it("PageKind is exactly 'flow-builder' | 'data-view'", () => {
    const fb: PageKind = "flow-builder";
    const dv: PageKind = "data-view";

    expect(fb).toBe("flow-builder");
    expect(dv).toBe("data-view");
  });
});

// ---------------------------------------------------------------------------
// All 5 commands are representable as SlashCommand
// ---------------------------------------------------------------------------

describe("SlashCommand — all 5 required commands", () => {
  const commandFixtures: Array<{
    name: string;
    aliases: string[];
    intent: ChatIntent;
    availableOn: PageKind[];
  }> = [
    {
      name: "search",
      aliases: ["s", "find"],
      intent: "search",
      availableOn: ["flow-builder", "data-view"],
    },
    {
      name: "flow",
      aliases: ["f", "create"],
      intent: "flow-create",
      availableOn: ["flow-builder"],
    },
    {
      name: "explain",
      aliases: ["e", "what"],
      intent: "explain",
      availableOn: ["flow-builder", "data-view"],
    },
    {
      name: "debug",
      aliases: ["d"],
      intent: "debug",
      availableOn: ["flow-builder"],
    },
    {
      name: "health",
      aliases: ["h", "status"],
      intent: "health",
      availableOn: ["flow-builder", "data-view"],
    },
  ];

  it.each(commandFixtures)(
    "$name command has correct intent $intent",
    ({ name, aliases, intent, availableOn }) => {
      const cmd: SlashCommand = {
        name,
        aliases,
        description: `Test ${name}`,
        usage: `/${name}`,
        intent,
        availableOn,
        parse: (args) => ({ intent, content: args, params: {} }),
      };

      expect(cmd.name).toBe(name);
      expect(cmd.intent).toBe(intent);
      expect(cmd.availableOn).toEqual(availableOn);
    },
  );

  it("parse function on a SlashCommand returns SlashCommandResult", () => {
    const cmd: SlashCommand = {
      name: "search",
      aliases: ["s", "find"],
      description: "Search the knowledge graph",
      usage: "/search <query>",
      intent: "search",
      availableOn: ["flow-builder", "data-view"],
      parse: (args) => ({
        intent: "search",
        content: args,
        params: { query: args },
      }),
    };

    const result = cmd.parse("drones in sector 7");
    expect(result.intent).toBe("search");
    expect(result.content).toBe("drones in sector 7");
    expect(result.params).toEqual({ query: "drones in sector 7" });
  });
});
