// Code generated by Tester Agent. DO NOT EDIT.
// Task: GraphQL data transformation tests
// Builder must make these tests pass without modification.

import { describe, it, expect } from "vitest";
import { transformPathSearchResult } from "./graphTransform";

describe("graphTransform", () => {
  describe("transformPathSearchResult", () => {
    it("should transform backend entities to GraphEntity array", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.property.name",
                object: "Drone 001",
              },
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.property.status",
                object: "active",
              },
            ],
          },
        ],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result).toHaveLength(1);
      expect(result[0].id).toBe("c360.ops.robotics.gcs.drone.001");
      expect(result[0].idParts).toEqual({
        org: "c360",
        platform: "ops",
        domain: "robotics",
        system: "gcs",
        type: "drone",
        instance: "001",
      });
      expect(result[0].properties).toHaveLength(2);
    });

    it("should convert triples with literal objects to properties", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.property.name",
                object: "Drone 001",
              },
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "telemetry.sensor.altitude",
                object: 150.5,
              },
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.status.active",
                object: true,
              },
            ],
          },
        ],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result[0].properties).toHaveLength(3);
      expect(result[0].properties[0]).toMatchObject({
        predicate: "core.property.name",
        object: "Drone 001",
      });
      expect(result[0].properties[1]).toMatchObject({
        predicate: "telemetry.sensor.altitude",
        object: 150.5,
      });
      expect(result[0].properties[2]).toMatchObject({
        predicate: "core.status.active",
        object: true,
      });
      expect(result[0].outgoing).toHaveLength(0);
      expect(result[0].incoming).toHaveLength(0);
    });

    it("should convert triples with 6-part entity IDs to relationships", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "fleet.membership.current",
                object: "c360.ops.robotics.gcs.fleet.alpha",
              },
            ],
          },
        ],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result[0].properties).toHaveLength(0);
      expect(result[0].outgoing).toHaveLength(1);
      expect(result[0].outgoing[0]).toMatchObject({
        sourceId: "c360.ops.robotics.gcs.drone.001",
        targetId: "c360.ops.robotics.gcs.fleet.alpha",
        predicate: "fleet.membership.current",
      });
      expect(result[0].outgoing[0].id).toBe(
        "c360.ops.robotics.gcs.drone.001:fleet.membership.current:c360.ops.robotics.gcs.fleet.alpha",
      );
    });

    it("should handle edges and create bidirectional relationships", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [],
          },
          {
            id: "c360.ops.robotics.gcs.fleet.alpha",
            triples: [],
          },
        ],
        edges: [
          {
            subject: "c360.ops.robotics.gcs.drone.001",
            predicate: "fleet.membership.current",
            object: "c360.ops.robotics.gcs.fleet.alpha",
          },
        ],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result).toHaveLength(2);

      const drone = result.find(
        (e) => e.id === "c360.ops.robotics.gcs.drone.001",
      );
      const fleet = result.find(
        (e) => e.id === "c360.ops.robotics.gcs.fleet.alpha",
      );

      expect(drone?.outgoing).toHaveLength(1);
      expect(drone?.outgoing[0]).toMatchObject({
        sourceId: "c360.ops.robotics.gcs.drone.001",
        targetId: "c360.ops.robotics.gcs.fleet.alpha",
        predicate: "fleet.membership.current",
      });

      expect(fleet?.incoming).toHaveLength(1);
      expect(fleet?.incoming[0]).toMatchObject({
        sourceId: "c360.ops.robotics.gcs.drone.001",
        targetId: "c360.ops.robotics.gcs.fleet.alpha",
        predicate: "fleet.membership.current",
      });
    });

    it("should handle multiple entities with cross-references", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.property.name",
                object: "Drone 001",
              },
            ],
          },
          {
            id: "c360.ops.robotics.gcs.drone.002",
            triples: [
              {
                subject: "c360.ops.robotics.gcs.drone.002",
                predicate: "core.property.name",
                object: "Drone 002",
              },
            ],
          },
          {
            id: "c360.ops.robotics.gcs.fleet.alpha",
            triples: [],
          },
        ],
        edges: [
          {
            subject: "c360.ops.robotics.gcs.drone.001",
            predicate: "fleet.membership.current",
            object: "c360.ops.robotics.gcs.fleet.alpha",
          },
          {
            subject: "c360.ops.robotics.gcs.drone.002",
            predicate: "fleet.membership.current",
            object: "c360.ops.robotics.gcs.fleet.alpha",
          },
        ],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result).toHaveLength(3);

      const fleet = result.find(
        (e) => e.id === "c360.ops.robotics.gcs.fleet.alpha",
      );
      expect(fleet?.incoming).toHaveLength(2);
      expect(fleet?.incoming.map((r) => r.sourceId)).toContain(
        "c360.ops.robotics.gcs.drone.001",
      );
      expect(fleet?.incoming.map((r) => r.sourceId)).toContain(
        "c360.ops.robotics.gcs.drone.002",
      );
    });

    it("should return empty array for empty result", () => {
      const backendResult = {
        entities: [],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result).toEqual([]);
    });

    it("should handle entities with no triples", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [],
          },
        ],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result).toHaveLength(1);
      expect(result[0].properties).toHaveLength(0);
      expect(result[0].outgoing).toHaveLength(0);
      expect(result[0].incoming).toHaveLength(0);
    });

    it("should distinguish between entity references and literal strings", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.property.manufacturer",
                object: "DJI Enterprise",
              },
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.property.model",
                object: "M30T",
              },
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "fleet.membership.current",
                object: "c360.ops.robotics.gcs.fleet.alpha",
              },
            ],
          },
        ],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result[0].properties).toHaveLength(2);
      expect(result[0].properties.map((p) => p.predicate)).toContain(
        "core.property.manufacturer",
      );
      expect(result[0].properties.map((p) => p.predicate)).toContain(
        "core.property.model",
      );

      expect(result[0].outgoing).toHaveLength(1);
      expect(result[0].outgoing[0].predicate).toBe("fleet.membership.current");
    });

    it("should handle malformed entity IDs (less than 6 parts)", () => {
      const backendResult = {
        entities: [
          {
            id: "invalid.id",
            triples: [
              {
                subject: "invalid.id",
                predicate: "core.property.name",
                object: "Invalid Entity",
              },
            ],
          },
        ],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result).toHaveLength(1);
      expect(result[0].id).toBe("invalid.id");
      expect(result[0].idParts).toMatchObject({
        org: "invalid",
        platform: "id",
        domain: "unknown",
        system: "unknown",
        type: "unknown",
        instance: "unknown",
      });
    });

    it("should handle entity references that are not in the entities list", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "fleet.membership.current",
                object: "c360.ops.robotics.gcs.fleet.beta",
              },
            ],
          },
        ],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result).toHaveLength(1);
      expect(result[0].outgoing).toHaveLength(1);
      expect(result[0].outgoing[0].targetId).toBe(
        "c360.ops.robotics.gcs.fleet.beta",
      );
    });

    it("should handle numeric and boolean object values correctly", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "telemetry.sensor.battery",
                object: 85,
              },
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "telemetry.sensor.temperature",
                object: 22.5,
              },
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.status.armed",
                object: false,
              },
            ],
          },
        ],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result[0].properties).toHaveLength(3);
      expect(result[0].properties[0].object).toBe(85);
      expect(result[0].properties[1].object).toBe(22.5);
      expect(result[0].properties[2].object).toBe(false);
      expect(result[0].outgoing).toHaveLength(0);
    });

    it("should not create relationships for non-6-part string objects", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.property.notes",
                object: "some.string.with.dots.but.not.six",
              },
              {
                subject: "c360.ops.robotics.gcs.drone.001",
                predicate: "core.property.shortId",
                object: "abc.def",
              },
            ],
          },
        ],
        edges: [],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result[0].properties).toHaveLength(2);
      expect(result[0].outgoing).toHaveLength(0);
    });

    it("should handle self-referential edges", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [],
          },
        ],
        edges: [
          {
            subject: "c360.ops.robotics.gcs.drone.001",
            predicate: "core.relationship.parent",
            object: "c360.ops.robotics.gcs.drone.001",
          },
        ],
      };

      const result = transformPathSearchResult(backendResult);

      expect(result).toHaveLength(1);
      expect(result[0].outgoing).toHaveLength(1);
      expect(result[0].incoming).toHaveLength(1);
      expect(result[0].outgoing[0].sourceId).toBe(
        "c360.ops.robotics.gcs.drone.001",
      );
      expect(result[0].outgoing[0].targetId).toBe(
        "c360.ops.robotics.gcs.drone.001",
      );
    });

    it("should create unique relationship IDs for each edge", () => {
      const backendResult = {
        entities: [
          {
            id: "c360.ops.robotics.gcs.drone.001",
            triples: [],
          },
          {
            id: "c360.ops.robotics.gcs.fleet.alpha",
            triples: [],
          },
        ],
        edges: [
          {
            subject: "c360.ops.robotics.gcs.drone.001",
            predicate: "fleet.membership.current",
            object: "c360.ops.robotics.gcs.fleet.alpha",
          },
          {
            subject: "c360.ops.robotics.gcs.drone.001",
            predicate: "fleet.membership.previous",
            object: "c360.ops.robotics.gcs.fleet.alpha",
          },
        ],
      };

      const result = transformPathSearchResult(backendResult);

      const drone = result.find(
        (e) => e.id === "c360.ops.robotics.gcs.drone.001",
      );
      expect(drone?.outgoing).toHaveLength(2);
      expect(drone?.outgoing[0].id).not.toBe(drone?.outgoing[1].id);
      expect(drone?.outgoing[0].id).toContain("fleet.membership.current");
      expect(drone?.outgoing[1].id).toContain("fleet.membership.previous");
    });
  });
});
