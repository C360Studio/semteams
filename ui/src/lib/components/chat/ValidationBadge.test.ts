// Code generated by Tester Agent. DO NOT EDIT.
// Task: Chat UI Phase 2 — ValidationBadge component tests
// Builder must make these tests pass without modification.

import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/svelte";
import ValidationBadge from "./ValidationBadge.svelte";

// ---------------------------------------------------------------------------
// Presence
// ---------------------------------------------------------------------------

describe("ValidationBadge — presence", () => {
  it("should have data-testid='validation-badge'", () => {
    render(ValidationBadge, { props: { status: "valid" } });

    expect(screen.getByTestId("validation-badge")).toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Valid status
// ---------------------------------------------------------------------------

describe("ValidationBadge — valid status", () => {
  it("should show 'Valid' text or a check indicator for valid status", () => {
    render(ValidationBadge, { props: { status: "valid" } });

    const badge = screen.getByTestId("validation-badge");
    // Accept "Valid", "✓", "✔", or aria-label containing "valid"
    const hasValidIndicator =
      /valid|✓|✔|check/i.test(badge.textContent ?? "") ||
      /valid/i.test(badge.getAttribute("aria-label") ?? "");
    expect(hasValidIndicator).toBe(true);
  });
});

// ---------------------------------------------------------------------------
// Warnings status
// ---------------------------------------------------------------------------

describe("ValidationBadge — warnings status", () => {
  it("should show warning count for warnings status", () => {
    render(ValidationBadge, {
      props: { status: "warnings", warningCount: 3 },
    });

    expect(screen.getByTestId("validation-badge")).toHaveTextContent("3");
  });

  it("should handle zero warning count", () => {
    render(ValidationBadge, {
      props: { status: "warnings", warningCount: 0 },
    });

    expect(screen.getByTestId("validation-badge")).toBeInTheDocument();
  });

  it("should handle warnings status without a count prop", () => {
    // Should not throw when warningCount is omitted
    expect(() =>
      render(ValidationBadge, { props: { status: "warnings" } }),
    ).not.toThrow();
  });
});

// ---------------------------------------------------------------------------
// Errors status
// ---------------------------------------------------------------------------

describe("ValidationBadge — errors status", () => {
  it("should show error count for errors status", () => {
    render(ValidationBadge, {
      props: { status: "errors", errorCount: 5 },
    });

    expect(screen.getByTestId("validation-badge")).toHaveTextContent("5");
  });

  it("should handle zero error count", () => {
    render(ValidationBadge, {
      props: { status: "errors", errorCount: 0 },
    });

    expect(screen.getByTestId("validation-badge")).toBeInTheDocument();
  });

  it("should handle errors status without a count prop", () => {
    // Should not throw when errorCount is omitted
    expect(() =>
      render(ValidationBadge, { props: { status: "errors" } }),
    ).not.toThrow();
  });
});

// ---------------------------------------------------------------------------
// Table-driven: status × expected content
// ---------------------------------------------------------------------------

describe("ValidationBadge — table-driven status", () => {
  const cases = [
    {
      description: "valid status — badge renders",
      props: { status: "valid" as const },
      expectCount: null,
    },
    {
      description: "warnings status with count 1",
      props: { status: "warnings" as const, warningCount: 1 },
      expectCount: "1",
    },
    {
      description: "warnings status with count 10",
      props: { status: "warnings" as const, warningCount: 10 },
      expectCount: "10",
    },
    {
      description: "errors status with count 2",
      props: { status: "errors" as const, errorCount: 2 },
      expectCount: "2",
    },
    {
      description: "errors status with count 99",
      props: { status: "errors" as const, errorCount: 99 },
      expectCount: "99",
    },
  ];

  it.each(cases)("$description", ({ props, expectCount }) => {
    render(ValidationBadge, { props });

    const badge = screen.getByTestId("validation-badge");
    expect(badge).toBeInTheDocument();

    if (expectCount !== null) {
      expect(badge).toHaveTextContent(expectCount);
    }
  });
});
