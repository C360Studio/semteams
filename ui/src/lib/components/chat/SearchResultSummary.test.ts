// Code generated by Tester Agent. DO NOT EDIT.
// Task: chat-redesign-phase-4 — Search and Graph Tools (Server-Side)
// Builder must make these tests pass without modification.
//
// NOTE: SearchResultSummary.svelte does not yet exist.
// All tests here will fail until the Builder creates that component.
//
// Phase 4 SearchResultAttachment shape (expanded from original types):
//   kind: "search-result"
//   query: string
//   results: Array<{ id: string; label: string; type: string; domain: string; score?: number }>
//   totalCount: number
//
// Component props:
//   attachment: SearchResultAttachment (phase 4 shape)
//   onViewEntity?: (entityId: string) => void

import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/svelte";
import userEvent from "@testing-library/user-event";
import SearchResultSummary from "./SearchResultSummary.svelte";

// ---------------------------------------------------------------------------
// Fixtures
// ---------------------------------------------------------------------------

// Phase 4 expanded SearchResultAttachment shape
interface SearchResultAttachmentV4 {
  kind: "search-result";
  query: string;
  results: Array<{
    id: string;
    label: string;
    type: string;
    domain: string;
    score?: number;
  }>;
  totalCount: number;
}

function makeAttachment(
  overrides: Partial<SearchResultAttachmentV4> = {},
): SearchResultAttachmentV4 {
  return {
    kind: "search-result",
    query: "drones near border",
    results: [
      {
        id: "c360.ops.robotics.gcs.drone.001",
        label: "001",
        type: "drone",
        domain: "robotics",
      },
      {
        id: "c360.ops.robotics.gcs.drone.002",
        label: "002",
        type: "drone",
        domain: "robotics",
      },
      {
        id: "c360.sec.border.surveillance.sensor.001",
        label: "001",
        type: "sensor",
        domain: "border",
      },
    ],
    totalCount: 3,
    ...overrides,
  };
}

function makeEmptyAttachment(): SearchResultAttachmentV4 {
  return {
    kind: "search-result",
    query: "nonexistent-thing",
    results: [],
    totalCount: 0,
  };
}

// ---------------------------------------------------------------------------
// Rendering — root element and testid
// ---------------------------------------------------------------------------

describe("SearchResultSummary — root element", () => {
  it("renders with data-testid='search-result-summary'", () => {
    render(SearchResultSummary, {
      props: { attachment: makeAttachment() },
    });

    expect(screen.getByTestId("search-result-summary")).toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Rendering — query text
// ---------------------------------------------------------------------------

describe("SearchResultSummary — shows query text", () => {
  it("displays the search query string", () => {
    render(SearchResultSummary, {
      props: { attachment: makeAttachment({ query: "border patrol sensors" }) },
    });

    expect(screen.getByText(/border patrol sensors/i)).toBeInTheDocument();
  });

  it("displays different query strings correctly", () => {
    render(SearchResultSummary, {
      props: { attachment: makeAttachment({ query: "maritime vessels" }) },
    });

    expect(screen.getByText(/maritime vessels/i)).toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Rendering — result count
// ---------------------------------------------------------------------------

describe("SearchResultSummary — shows result count", () => {
  it("displays the totalCount", () => {
    render(SearchResultSummary, {
      props: { attachment: makeAttachment({ totalCount: 3 }) },
    });

    expect(screen.getByTestId("search-result-summary")).toHaveTextContent("3");
  });

  it("displays large totalCounts correctly", () => {
    render(SearchResultSummary, {
      props: {
        attachment: makeAttachment({
          totalCount: 142,
          results: [
            {
              id: "c360.ops.robotics.gcs.drone.001",
              label: "001",
              type: "drone",
              domain: "robotics",
            },
          ],
        }),
      },
    });

    expect(screen.getByTestId("search-result-summary")).toHaveTextContent(
      "142",
    );
  });
});

// ---------------------------------------------------------------------------
// Rendering — result items
// ---------------------------------------------------------------------------

describe("SearchResultSummary — renders result items", () => {
  it("renders each result item", () => {
    const attachment = makeAttachment();
    render(SearchResultSummary, { props: { attachment } });

    // Each result should be present in some form
    for (const result of attachment.results) {
      expect(
        screen.getByTestId(`search-result-item-${result.id}`),
      ).toBeInTheDocument();
    }
  });

  it("shows label or id for each result", () => {
    const attachment = makeAttachment({
      results: [
        {
          id: "c360.ops.robotics.gcs.drone.omega",
          label: "omega",
          type: "drone",
          domain: "robotics",
        },
      ],
    });
    render(SearchResultSummary, { props: { attachment } });

    expect(screen.getByTestId("search-result-summary")).toHaveTextContent(
      /omega/i,
    );
  });

  it("shows entity type for each result", () => {
    const attachment = makeAttachment({
      results: [
        {
          id: "c360.sec.border.surveillance.sensor.001",
          label: "001",
          type: "sensor",
          domain: "border",
        },
      ],
    });
    render(SearchResultSummary, { props: { attachment } });

    expect(screen.getByTestId("search-result-summary")).toHaveTextContent(
      /sensor/i,
    );
  });

  it("renders three items for a three-result attachment", () => {
    const attachment = makeAttachment();
    render(SearchResultSummary, { props: { attachment } });

    const items = screen.getAllByRole("button");
    // All 3 results should produce clickable elements
    expect(items.length).toBeGreaterThanOrEqual(3);
  });
});

// ---------------------------------------------------------------------------
// Interaction — clicking calls onViewEntity
// ---------------------------------------------------------------------------

describe("SearchResultSummary — clicking result calls onViewEntity", () => {
  it("calls onViewEntity with the entity id when a result is clicked", async () => {
    const onViewEntity = vi.fn();
    const user = userEvent.setup();

    const attachment = makeAttachment({
      results: [
        {
          id: "c360.ops.robotics.gcs.drone.click-me",
          label: "click-me",
          type: "drone",
          domain: "robotics",
        },
      ],
    });

    render(SearchResultSummary, {
      props: { attachment, onViewEntity },
    });

    const item = screen.getByTestId(
      "search-result-item-c360.ops.robotics.gcs.drone.click-me",
    );
    await user.click(item);

    expect(onViewEntity).toHaveBeenCalledOnce();
    expect(onViewEntity).toHaveBeenCalledWith(
      "c360.ops.robotics.gcs.drone.click-me",
    );
  });

  it("calls onViewEntity with correct id for each distinct result", async () => {
    const onViewEntity = vi.fn();
    const user = userEvent.setup();

    const attachment = makeAttachment({
      results: [
        {
          id: "c360.ops.robotics.gcs.drone.alpha",
          label: "alpha",
          type: "drone",
          domain: "robotics",
        },
        {
          id: "c360.ops.robotics.gcs.drone.beta",
          label: "beta",
          type: "drone",
          domain: "robotics",
        },
      ],
    });

    render(SearchResultSummary, {
      props: { attachment, onViewEntity },
    });

    await user.click(
      screen.getByTestId(
        "search-result-item-c360.ops.robotics.gcs.drone.alpha",
      ),
    );
    expect(onViewEntity).toHaveBeenLastCalledWith(
      "c360.ops.robotics.gcs.drone.alpha",
    );

    await user.click(
      screen.getByTestId("search-result-item-c360.ops.robotics.gcs.drone.beta"),
    );
    expect(onViewEntity).toHaveBeenLastCalledWith(
      "c360.ops.robotics.gcs.drone.beta",
    );
  });
});

// ---------------------------------------------------------------------------
// Interaction — onViewEntity prop is optional
// ---------------------------------------------------------------------------

describe("SearchResultSummary — onViewEntity is optional", () => {
  it("does not throw when onViewEntity is not provided and result is clicked", async () => {
    const user = userEvent.setup();

    const attachment = makeAttachment({
      results: [
        {
          id: "c360.ops.robotics.gcs.drone.safe",
          label: "safe",
          type: "drone",
          domain: "robotics",
        },
      ],
    });

    render(SearchResultSummary, { props: { attachment } });

    await expect(
      user.click(
        screen.getByTestId(
          "search-result-item-c360.ops.robotics.gcs.drone.safe",
        ),
      ),
    ).resolves.not.toThrow();
  });
});

// ---------------------------------------------------------------------------
// Empty results — no results state
// ---------------------------------------------------------------------------

describe("SearchResultSummary — empty results", () => {
  it("shows a 'no results' message when results array is empty", () => {
    render(SearchResultSummary, {
      props: { attachment: makeEmptyAttachment() },
    });

    expect(screen.getByTestId("search-result-summary")).toHaveTextContent(
      /no results/i,
    );
  });

  it("still shows the query in no-results state", () => {
    render(SearchResultSummary, {
      props: {
        attachment: makeEmptyAttachment(),
      },
    });

    expect(screen.getByTestId("search-result-summary")).toHaveTextContent(
      /nonexistent-thing/i,
    );
  });

  it("does not render any result items when results is empty", () => {
    render(SearchResultSummary, {
      props: { attachment: makeEmptyAttachment() },
    });

    // No individual result items should be present
    expect(
      screen.queryByTestId(/^search-result-item-/),
    ).not.toBeInTheDocument();
  });
});

// ---------------------------------------------------------------------------
// Table-driven: result count display
// ---------------------------------------------------------------------------

describe("SearchResultSummary — table-driven count display", () => {
  it.each([
    { totalCount: 0, label: "0 results" },
    { totalCount: 1, label: "1 result" },
    { totalCount: 10, label: "10 results" },
  ])("totalCount=$totalCount shows count in summary", ({ totalCount }) => {
    const results =
      totalCount > 0
        ? [
            {
              id: "c360.ops.robotics.gcs.drone.001",
              label: "001",
              type: "drone",
              domain: "robotics",
            },
          ]
        : [];

    render(SearchResultSummary, {
      props: {
        attachment: makeAttachment({ results, totalCount }),
      },
    });

    expect(screen.getByTestId("search-result-summary")).toHaveTextContent(
      String(totalCount),
    );
  });
});
